import { useState } from "react";

const PlayerDetails = ({ player }) => {

    return (
        <div>
            insert player details here&nbsp;
            {player.player_name}
        </div>
    )
}

export default PlayerDetails
